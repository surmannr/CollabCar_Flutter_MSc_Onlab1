import 'package:flutter/material.dart';

class StepTwoSearch extends StatelessWidget {
  const StepTwoSearch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("data")],
    );
  }
}
